import React from 'react';
// Disabled for performance — kept as no-op to preserve imports.
export const CursorSplineRobot: React.FC<{ scene?: string }> = () => null;
export default CursorSplineRobot;